# ============================================
# PLANTILLA DE VARIABLES DE ENTORNO
# ============================================
# Copia este archivo a .env y configura tus valores
# NUNCA subas el archivo .env a Git

# === DOCKER HUB ===
DOCKERHUB_USER=s4lvaborjamoll
VERSION=latest

# === POSTGRESQL (Base de datos principal) ===
POSTGRES_USER=appuser
POSTGRES_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO
POSTGRES_DB=appdb

# === RABBITMQ (Cola de mensajes) ===
RABBITMQ_USER=rabbituser
RABBITMQ_PASS=CAMBIAR_POR_PASSWORD_SEGURO

# === MINIO (Almacenamiento S3) ===
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO

# === METABASE (Business Intelligence) ===
METABASE_DB_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO

# === LEGACY DATABASE (MariaDB) ===
LEGACY_DB_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO

# ============================================
# INSTRUCCIONES PARA GENERAR PASSWORDS SEGUROS
# ============================================
# Opción 1: Usar el script incluido
#   ./generate_secrets.sh
#
# Opción 2: Generar manualmente con openssl
#   openssl rand -base64 32
#
# Opción 3: Generar con pwgen
#   pwgen -s 32 1
